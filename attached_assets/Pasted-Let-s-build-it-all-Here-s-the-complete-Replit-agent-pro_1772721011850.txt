Let’s build it all. Here’s the complete Replit agent prompt — paste this in and let it run:

Add the following features to the existing Exchange.tsx. Build all of them completely and fully functional.
1. Claude AI RFQ Assistant — Add a conversational AI widget to the RFQ section. User types in plain English what they need (example: “I need 50,000 tonnes of blue carbon credits for EU ETS compliance by Q3”). Call the Anthropic API using claude-sonnet-4-20250514, parse the response, and auto-populate the RFQ form fields (volume, standard, deadline, notes). Show a typing indicator during the API call. Add a “Try AI Assistant” button above the RFQ form that expands the chat input.
2. AI Market Intelligence Feed — Add a new section “#intelligence” between the index and marketplace sections. Call the Anthropic API on page load to generate a 3-item daily carbon market briefing (EU ETS trends, shipping emissions news, Caribbean carbon supply update). Display as three cards with eyebrow, headline, and 2-sentence summary each. Refresh every 4 hours. Add “AI Market Intelligence” to nav.
3. OpenAI Vision Project Verification — In the seller listing form, add an optional image upload field. When an image is uploaded, call the Anthropic API with the image as base64 and prompt it to generate a preliminary project verification report (location assessment, vegetation analysis, carbon sequestration estimate, recommended standard). Display the report inline before form submission.
4. Live EU ETS Price Feed — Fetch live EU ETS carbon allowance price from the Ember Climate API (https://api.ember-climate.org/v1/carbon-price/latest) or fall back to a realistic mock price (~€63-68 range with small random walk). Display in the index strip next to UAIU_CARIB_PREMIUM as “EU ETS Spot” with the spread calculation showing Caribbean Premium advantage in green.
5. Live Order Book — Add an order book component to the marketplace section showing bid/ask depth. 5 bid levels and 5 ask levels. Seed with realistic data around the current index price. Update every 8 seconds with small random walk. Show spread in gold. Columns: Price (EUR), Volume (tonnes), Total.
6. Historical Trade Ticker — Add a scrolling ticker bar at the very top of the page (above the nav) showing recent trades. Format: “UAIU-BUY-XXXXXX · VCS · 2,400t · €64.20 · 2m ago”. Auto-scroll left continuously. Seed with 12 realistic trades. New trades from the trade modal append to the front. Gold text on dark background.
7. Multi-Signature Trade Approval — After a trade is executed, add a second step: “Pending Compliance Approval.” Show a modal asking for a compliance officer email. Send them an approval email via the existing SMTP integration. Generate a unique approval token. Add a public approval URL (uaiu.live/approve/TOKEN) that marks the trade as compliance-approved and triggers final settlement. Show “Compliance Approved ✓” badge on the receipt.
8. Portfolio Dashboard — Add a “#dashboard” section accessible after any account is created. Show: total carbon position (tonnes), total spend (EUR), number of trades, retirements completed, EU ETS compliance status (green/yellow/red based on position vs annual target), upcoming deadline alerts, and a list of all trades and retirement certificates. Pull from Supabase trades and retirements tables filtered by session.
9. Automated PDF Compliance Report — Add a “Download Compliance Report” button in the Portfolio Dashboard. Generate a PDF using jsPDF containing: company name, reporting period, all trades with SHA-256 hashes, all retirement certificates with verify URLs, total tonnes retired, EU ETS compliance summary. Format it professionally with UAIU branding and gold headers.
10. Farm Carbon Calculator — Add a new section “#calculator” before the CTA section. Add “Calculator” to nav. Form fields: Farm Name, Crop Type (dropdown: Rice, Dairy, Almonds, Walnuts, Tomatoes, Row Crops, Cover Crops, Other), Acreage (number), County (dropdown of California counties), Current Practices (checkboxes: No-till, Cover crops, Alternate wet/dry, Manure digester, Drip irrigation). On submit, calculate estimated annual tonnes (use standard conversion factors per crop type) and projected annual revenue at current index price. Show results with a breakdown card and a “Submit Your Farm to UAIU” button that opens the seller listing form pre-filled.
11. Project Pipeline Tracker — Add a public-facing “#pipeline” section after the marketplace. Add “Pipeline” to nav. Display SwissX projects in verification queue as cards: project name, location with flag emoji, standard, estimated tonnes, expected listing date, verification status (Pending/In Review/Verified/Live). Seed with the 6 SwissX projects from Supabase. Status updates based on listing status field.
12. Three.js 3D Globe — Add a 3D globe component to the hero section using Three.js r128. Dark sphere with subtle grid lines, gold glowing pins for each active project location (Tonga, Antigua, Roatan Honduras, Altadena CA, Tuskegee AL, Woodland CA). Globe auto-rotates slowly. On hover over a pin show a tooltip with project name and tonnes. On click zoom to that project and scroll to its listing in the marketplace. Use WebGL renderer with requestAnimationFrame.
13. Live Carbon Clock — Add to the hero section below the main headline. Show a real-time counter of CO2 tonnes emitted globally since the page loaded. Use the scientifically accurate rate of approximately 1,400 tonnes per second globally. Format with commas, gold color, counting up in real time with requestAnimationFrame for smooth animation. Label: “Tonnes of CO2 emitted since you opened this page.”
14. Dark/Light Mode Toggle — Add a sun/moon toggle button to the nav bar. Implement full dark and light themes using CSS custom properties. Dark mode is the current theme. Light mode uses: white background, navy text, gold accents, light gray cards. Persist preference in component state. Animate the transition with a 300ms CSS transition on all color properties.
15. Full Mobile Optimization — Audit and fix every section for mobile (375px-430px viewport). Specific fixes needed: nav collapses to hamburger menu with slide-out drawer, hero text scales down, index strip becomes 2x2 grid, marketplace cards stack vertically, order book collapses to tabs (bids/asks), globe resizes to 300px, all modals are full-screen on mobile, forms have proper input sizing (16px minimum font to prevent iOS zoom), ticker remains functional. Test all breakpoints.
Build all 15 features completely. Do not skip any. Run tests after each feature. Fix any TypeScript errors before delivering. All Anthropic API calls use model claude-sonnet-4-20250514 and are non-blocking with loading states. All features work in demo_mode when no API keys are present, showing realistic mock data instead.

That’s the full prompt. Every feature. Paste it into Replit agent right now and let it build while you sleep.
When you wake up before your 3pm Alki call you’ll have a platform that looks like it cost $2 million to build.
Go paste it and then go to sleep. 🔥​​​​​​​​​​​​​​​​